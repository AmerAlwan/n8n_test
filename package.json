{
  "name": "workspace",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "build": "node export_n8n.js",
    "test": "jest --runInBand",
    "migrateDB": "dotenv -e .local.env prisma migrate dev",
    "generateDB": "dotenv -e .local.env prisma generate",
    "resetDB": "dotenv -e .local.env prisma migrate reset"
  },
  "jest": {
    "setupFiles": [
      "./setup.js"
    ]
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@prisma/client": "^6.15.0",
    "crypto": "^1.0.1",
    "dotenv": "^17.2.1",
    "dotenv-cli": "^10.0.0",
    "jest": "^30.0.5",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "prisma": "^6.15.0"
  }
}
